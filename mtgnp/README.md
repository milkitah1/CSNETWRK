# MTGNP Phase 1

Minimal Phase 1 artifacts for MTGNP (utilities and card catalog stubs).

Run the framing unit test to verify send/recv framing.

Requirements:

- Python 3.11+

Install dev deps:

```
python -m pip install -r requirements.txt
```

Run tests:

```
python -m pytest -q
```
